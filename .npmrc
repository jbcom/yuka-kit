# Public package: resolve from the public registry. The private Gitea registry
# this was extracted from is unreachable for anyone outside the network.
registry=https://registry.npmjs.org/
provenance=true
